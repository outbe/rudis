/target
*.swp
*.swo
*.pyc
__pycache__/
.DS_Store
piolium

# Ruler-generated local tool output.
.codex/
.claude/
.cursor/
.grok/

# Pi project-local packages (if using pi install -l)
.serena
/.vscode

.idea
/e2e/evm/out
/e2e/evm/cache

# codegraph index (local code-intelligence cache)
/.codegraph

# Barretenberg CRS downloaded/generated for local proving and verification.
/.bb-crs/
/contracts/account-abstraction/lib/kernel/broadcast
/.ruler

target-docker/

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key
.beads/proxieddb/
.beads

# Local environment settings; keep distributable examples.
.env
.env.*
!.env.example
!.env.sample
!.env.template
validator.env

# Local network configuration and generated deployment directories.
/network.yaml
/keys/
/testnet/
/rehearsal/
/scripts/keys/
/scripts/network.yaml
/validator-*/
/scripts/validator-*/

# Validator private keys, including outputs placed outside the default directories.
**/validator-*/evm-key.hex
**/validator-*/signing-key.hex
**/validator-*/reth-p2p-secret.hex
**/validator-*/radicle/keys/radicle
